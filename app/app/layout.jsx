export const metadata = {
  title: "HerEra — Confidence in Every Rep",
  description: "India's first detachable coverup gym leggings for women.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
